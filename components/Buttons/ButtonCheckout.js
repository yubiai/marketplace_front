import { Button } from "@chakra-ui/react";

const ButtonCheckout = (props) => {
  const createTransaction = () => {

  };
  
  return (
    <>
      <Button bc={'yb.1'} color={'white'} onClick={createTransaction}>Checkout</Button>
    </>
  );
};

export default ButtonCheckout;
