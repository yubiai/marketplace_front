


// Pedir datos al backend
/*   const refreshNoti = async () => {
    setData(null)
    await notiService
      .getNotiFalseByUserId(userId, token)
      .then((res) => {
        console.log(res.data.totalItems, 'Cantidad de notifications')
        setData(res.data)
      })
      .catch((err) => {
        console.log(err)
      })
  } */